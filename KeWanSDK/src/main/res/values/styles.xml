<resources>

    <style name="kewan_popupDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground"> @android:color/transparent </item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:backgroundDimAmount">0.1</item><!-- 灰度 -->
    </style>
    <style name="kewan_mystyle" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <!-- 除去title -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 除去背景色 , 去掉背景色边框也就去掉了，在你的层中设置背景色就可以了　 -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
    <!-- dialog样式 -->

    <style name="kewan_dialog" parent="android:style/Theme.Dialog">
        <!-- 透明背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 无边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <!-- 对话框开启和关闭的动画 -->
    <style name="kewan_style_item" parent="android:Animation">
        <!-- 从屏幕底部弹出 -->
        <item name="@android:windowEnterAnimation">@anim/kewan_dialog_enter</item>
        <!-- 从弹出的位置收回 -->
        <item name="@android:windowExitAnimation">@anim/kewan_dialog_exit</item>
    </style>
</resources>
